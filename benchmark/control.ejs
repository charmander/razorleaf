<!DOCTYPE html>

<html>
	<head>
		<meta charset="utf-8">

		<title><%= title %> — A demo</title>

		<% stylesheets.forEach(function(stylesheet) { %>
			<link rel="stylesheet" type="text/css" href="<%= stylesheet.url %>">
		<% }); %>

		<% scripts.forEach(function(script) { %>
			<script type="text/javascript" src="<%= script.url %>"<% if(script.async) { -%> async<% } %>></script>
		<% }); %>
	</head>
	<body>
		<h1 id="title">An example</h1>

		<p id="content">Hello, <em>world!</em></p>
	</body>
</html>
